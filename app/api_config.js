const config = require('../config/config');
const baseUrl = config.baseUrl;

// dev
const baseWriteUrl = baseUrl + "/write/api/v1/";
const baseReadUrl = baseUrl + "/read/api/v1/";


const API = {

}

module.exports = API;
